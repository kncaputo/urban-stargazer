import React, { useState, useEffect } from 'react';
import Card from '../Card';
import './Saved.scss';

const Saved = (props) => {
 
  return(
    <main>
      <section>
        
      </section>
    </main>
  )
}

export default Saved;
import { fetchPictureFromDate } from './index';
import { act, render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import userEvent from '@testing-library/user-event';
import { filteredImage1, filteredImage2 } from '../sampleData';

describe('apiCalls', () => {
  it('should call fetchPictureFromDate with a date to the correct endpoint', () => {
    
  });

  it('should throw an error if the response is not okay', () => {

  });

  it('should return the response.json() if the response is okay', () => {

  });
})